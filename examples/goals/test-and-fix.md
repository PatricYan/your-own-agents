Write tests for the function and run them. Fix any failures.

1. Read the implementation from the input
2. Write test cases covering main paths and edge cases
3. Run the tests using the shell
4. Fix any failures and re-run until all pass
5. Submit the test results
